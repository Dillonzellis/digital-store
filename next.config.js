/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains:[
      'localhost',
      'https://digital-store-production.up.railway.app/'
    ]
  },
};

module.exports = nextConfig;
